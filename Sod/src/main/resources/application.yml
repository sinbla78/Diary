spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/users?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: yhm75040611?

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update

  jackson:
    property-naming-strategy: SNAKE_CASE

  jwt:
    header: Authorization
    secret-key: your_actual_secret_key_here
    access-exp: 3600 # Access token expiration time in seconds (e.g., 1 hour)
    refresh-exp: 86400 # Refresh token expiration time in seconds (e.g., 1 day)
    prefix: Bearer

  redis:
    host: localhost
    port: 6379